---
apiVersion: projects.argocd.crossplane.io/v1alpha1
kind: Project
metadata:
  name: example-project
  labels:
    global-project: "true"
spec:
  forProvider: {}
  providerConfigRef:
    name: argocd-provider
